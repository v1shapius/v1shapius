# Discord Rating Bot

Discord бот для проведения рейтинговых матчей между игроками с системой Глико-2 и поддержкой форматов Bo1, Bo2, Bo3.

## 🚀 Возможности

- **Система матчей**: Поддержка форматов Bo1, Bo2, Bo3
- **Рейтинговая система**: Глико-2 с настраиваемыми параметрами
- **Контроль матчей**: Проверка голосовых каналов и трансляций
- **Подтверждение драфтов**: Через скриншоты и ссылки
- **Система штрафов**: Настраиваемые штрафы за рестарты
- **Мультиязычность**: Поддержка русского и английского языков
- **Сезоны**: Автоматическое управление сезонами
- **Масштабируемость**: Поддержка множества серверов

## 🏗️ Архитектура

```
├── bot.py                 # Основной файл бота
├── config/                # Конфигурация
├── cogs/                  # Модули бота (cogs)
├── models/                # Модели базы данных
├── database/              # Менеджер базы данных
├── locales/               # Файлы локализации
├── services/              # Бизнес-логика
├── utils/                 # Вспомогательные функции
├── docker-compose.yml     # Docker Compose конфигурация
└── requirements.txt       # Python зависимости
```

## 🛠️ Технологии

- **Python 3.11+** - Основной язык
- **Discord.py 2.x** - Discord API библиотека
- **PostgreSQL** - Основная база данных
- **Redis** - Кэширование и очереди
- **SQLAlchemy** - ORM для работы с БД
- **Docker** - Контейнеризация
- **Celery** - Фоновые задачи

## 📋 Требования

- Python 3.11+
- Docker и Docker Compose
- Discord Bot Token
- PostgreSQL 15+
- Redis 7+

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd discord-rating-bot
```

### 2. Настройка переменных окружения

```bash
cp .env.example .env
# Отредактируйте .env файл, добавив ваш Discord Bot Token
```

### 3. Запуск с Docker

```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f bot

# Остановка
docker-compose down
```

### 4. Ручная установка (без Docker)

```bash
# Установка зависимостей
pip install -r requirements.txt

# Настройка базы данных
# Создайте PostgreSQL базу данных и обновите DATABASE_URL в .env

# Запуск бота
python bot.py
```

## ⚙️ Конфигурация

### Основные настройки

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `DISCORD_TOKEN` | Discord Bot Token | Обязательно |
| `DEFAULT_LOCALE` | Язык по умолчанию | `en` |
| `DEFAULT_RESTART_PENALTY` | Штраф за рестарт (секунды) | `30` |
| `VOICE_CHANNEL_DELAY` | Задержка удаления голосового канала | `300` |
| `INITIAL_RATING` | Начальный рейтинг | `1500` |

### Настройка рейтинговой системы

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `K_FACTOR_NEW` | K-фактор для новых игроков | `40` |
| `K_FACTOR_ESTABLISHED` | K-фактор для опытных игроков | `20` |
| `ESTABLISHED_THRESHOLD` | Порог для "опытных" игроков | `30` |

## 🎮 Использование

### Основные команды

- `/match` - Создать новый матч
- `/matches` - Просмотр активных матчей
- `/rating` - Просмотр рейтинга
- `/leaderboard` - Таблица лидеров
- `/season` - Информация о сезоне

### Сценарий матча

1. **Создание матча**: Игрок создает матч с указанием формата и оппонента
2. **Присоединение**: Оба игрока присоединяются к голосовому каналу
3. **Подтверждение готовности**: Игроки подтверждают готовность
4. **Подтверждение драфта**: Игроки отправляют ссылки на драфт
5. **Выбор первого игрока**: Определение очередности
6. **Проведение игр**: Игроки проходят уровни
7. **Подтверждение результатов**: Ввод времени и количества рестартов
8. **Завершение**: Определение победителя и обновление рейтинга

## 🗄️ База данных

### Основные таблицы

- `players` - Информация об игроках
- `matches` - Данные о матчах
- `game_results` - Результаты отдельных игр
- `ratings` - История рейтингов
- `seasons` - Информация о сезонах
- `penalty_settings` - Настройки штрафов

### Миграции

```bash
# Создание миграции
alembic revision --autogenerate -m "Description"

# Применение миграций
alembic upgrade head
```

## 🔧 Разработка

### Структура проекта

```
cogs/
├── match_management.py    # Управление матчами
├── rating_system.py       # Рейтинговая система
├── voice_control.py       # Контроль голосовых каналов
├── admin.py              # Административные функции
└── draft_verification.py  # Проверка драфтов

services/
├── match_service.py       # Бизнес-логика матчей
├── rating_service.py      # Расчеты рейтинга
├── voice_service.py       # Работа с голосовыми каналами
└── notification_service.py # Система уведомлений
```

### Добавление новых команд

1. Создайте новый ког в папке `cogs/`
2. Реализуйте команды с декоратором `@app_commands.command`
3. Добавьте ког в `bot.py` в методе `setup_hook`

### Добавление новых языков

1. Создайте файл локализации в папке `locales/`
2. Добавьте переводы в `LocaleManager` в `locales/__init__.py`
3. Обновите `Config.DEFAULT_LOCALE` при необходимости

## 🐳 Docker

### Развертывание

```bash
# Продакшн
docker-compose --profile production up -d

# Разработка
docker-compose up -d postgres redis
docker-compose up bot
```

### Масштабирование

```bash
# Запуск нескольких инстансов бота
docker-compose up -d --scale bot=3
```

## 📊 Мониторинг

### Логи

- Логи бота: `logs/bot.log`
- Логи базы данных: `docker-compose logs postgres`
- Логи Redis: `docker-compose logs redis`

### Health Checks

```bash
# Проверка состояния сервисов
docker-compose ps

# Проверка здоровья базы данных
docker-compose exec postgres pg_isready -U bot_user -d discord_bot
```

## 🔒 Безопасность

- Все команды проверяют права доступа
- Валидация всех вводимых данных
- Логирование критических операций
- Изоляция данных между серверами

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 🆘 Поддержка

- Создайте Issue в GitHub
- Обратитесь к документации
- Проверьте логи для диагностики проблем

## 🔄 Обновления

```bash
# Обновление кода
git pull origin main

# Пересборка и перезапуск
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

---

**Примечание**: Убедитесь, что у вашего Discord бота есть необходимые права для создания голосовых каналов и управления ими.